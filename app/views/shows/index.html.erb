  <%= form_for @search, :url => shows_path, :html => {:method => :get} do |f| %>
    <%= f.label :name_contains %>
    <%= f.text_field :name_contains %><br />
    <%# f.label :comments_created_at_greater_than, 'With comments after' %>
    <%# f.datetime_select :comments_created_at_greater_than, :include_blank => true %><br />
    <!-- etc... -->
    <%= f.submit %>
  <% end %>
  <%= sort_link @search, :sortable_name, "Name" %>
  <%= sort_link @search, :first_aired_at %>
  <%= sort_link @search, :episode_count %>

<%= render :partial => "search_results", :locals => {:search_results => collection} %>
